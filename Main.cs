using System;

namespace Task_31
{
    class Program
    {
        static void Main()
        {
            // Array1D array1D = new Array1D(5, true);
            // Console.WriteLine($"Average: {array1D.GetAverage()}");
            // array1D.OutputArray();
            // array1D.Deduplicate();
            // array1D.OutputArray();
            // array1D.DeleteElementsOver100();
            // array1D.OutputArray();

            // Array2D array2D = new Array2D(3, 4, true);
            // array2D.OutputArray();
            // array2D.OutputArray();
            // Console.WriteLine();
            // array2D.OutputArray(true);
            // Console.WriteLine($"Average: {array2D.Average()}");

            // JaggedArray jaggedArray = new JaggedArray(3);
            // jaggedArray.Output();
            // Console.WriteLine($"Average: {jaggedArray.Average()}");
            // jaggedArray.PrintAverageByRows();
            // jaggedArray.ChangeEvenElements();
            // jaggedArray.Output();
        }
    }
}